import React from 'react';

const Title = () => (
        <div>
            <h1 className="title-container__title">Search Weather condition!</h1>
            <p className="title-container__subtitle">Find out temprature, conditions and other more...</p>
        </div>
    );

export default Title;